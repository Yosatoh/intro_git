# ndaary関数の数値への文字の代入方法
 y = np.array([0,1,0,1,1]
 y_named = np.array(['mozi0' 'mozi1'])[y]
	そのた例：
	from sklearn.datasets import make_circles
	X, y = make_circles(noise=0.25, factor = 0.5, random_state=1)
		#わかりやすいように,クラスを’blue' と'red'にするcfrom sklearn.da		y_named = np.array(['blue', 'red'])[y]
